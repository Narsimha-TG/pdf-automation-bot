# PDF Automation Bot

An automated tool built and verified by an Autonomous AI Agent workflow to extract text and tabular data.

## Usage
```bash
python main.py --input sample.pdf --output result.json
```
